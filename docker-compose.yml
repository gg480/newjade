version: "3.8"

services:
  jade-inventory:
    image: lrunningmjgoat/jade-inventory:latest
    container_name: jade-inventory
    ports:
      - "5000:5000"
    volumes:
      # Map all persistent data to a local directory on your NAS
      # - db/       → SQLite database
      # - images/   → User uploaded images
      # - logs/     → Application logs
      # Change ./jade-data to any path on your NAS, e.g. /volume1/docker/jade-data
      - ./jade-data:/app/data
    environment:
      - DATA_DIR=/app/data
      - NODE_ENV=production
    restart: unless-stopped

# Alternative: use a named volume instead of local directory
# volumes:
#   jade-data:
#     driver: local
